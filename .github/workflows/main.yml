name: Deploy to AKS

on:
  workflow_dispatch: 
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set up Azure CLI
        run: az extension add --name aks

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
         
      - name: Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
         login-server: koushikpk.azurecr.io
         username: ${{ secrets.ACR_USERNAME }}
         password: ${{ secrets.ACR_PASSWORD }}
         
      - name: Push frontend image to ACR
        working-directory: ./My-project
        run: |
          docker build -t koushikpk.azurecr.io/My-project .
          docker push koushikpk.azurecr.io/My-project

      - name: Set up Kubernetes CLI
        uses: azure/aks-set-context@v3
        with:
         resource-group: 'koushik'
         cluster-name: 'koushik-cluster'
        
      - name: Install kubectl
        run: |
         echo "deb https://apt.kubernetes.io/ kubernetes-xenial main" | sudo tee /etc/apt/sources.list.d/kubernetes.list
         curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
         sudo apt-get update
         sudo apt-get install -y kubectl
        shell: bash
        
      - name: Pull user_management image from ACR
        run: |
         az acr login --name koushikpk.azurecr.io
         docker pull koushikpk.azurecr.io/My-project
         docker tag koushikpk.azurecr.io/My-project
        shell: bash
       
      - name: Deploy app to AKS
        run: kubectl apply -f My-project/deployment.yaml
